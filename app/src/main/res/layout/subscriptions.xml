<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Trial Period Expires at 20/Sept/2016"
        android:id="@+id/trialPeriodTv"
        android:layout_gravity="center_horizontal" />

    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:id="@+id/pearImgView"
        android:src="@drawable/pear"
        android:visibility="gone"
        android:layout_gravity="center_horizontal" />

    <learn2.program.testshapesubsriptions.subscriptions.CustomViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="55dp"
        android:layout_marginTop="55dp"
        app:matchChildWidth="@+id/vg_cover" />
</LinearLayout>